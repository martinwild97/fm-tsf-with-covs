services:
  moirai:
    build: .
    ports:
      - "0.0.0.0:8080:8080"
    entrypoint: "./run.sh"
    deploy:
      resources:
        reservations:
          devices:
          - driver: nvidia
            count: all
            capabilities: [gpu]
